# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Playwright's saved storageState. Gitignored at the repo root, but prettier
# runs from this package and only reads the .gitignore next to it – so a local
# `npm run lint` after an e2e run would otherwise fail on a generated file.
/playwright/.auth/
